<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_balance_night"
    android:padding="20dp">

    <!-- Header: Label -->
    <TextView
        android:id="@+id/label_solde"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Votre solde"
        android:textSize="14sp"
        android:textColor="#B3FFFFFF" 
        android:textStyle="normal"
        android:fontFamily="sans-serif-medium"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />
        
    <!-- Header: Icon (Eye/Refresh placeholder) - Right aligned -->
    <ImageView
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@android:drawable/ic_menu_view"
        android:alpha="0.7"
        android:tint="#FFFFFF"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true" />

    <!-- Center: Balance Value -->
    <TextView
        android:id="@+id/balance_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0 FCFA"
        android:textSize="36sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:fontFamily="sans-serif-light"
        android:letterSpacing="-0.02"
        android:layout_centerVertical="true"
        android:layout_marginTop="8dp"
        android:layout_alignParentStart="true" />

    <!-- Footer: Greeting and Icon -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_alignParentBottom="true">

        <!-- Time Icon placeholder (sun/moon) -->
        <!-- Since we don't have vectors yet, using a small text emoji or standard icon -->
        <TextView
            android:id="@+id/time_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🌙" 
            android:textSize="14sp"
            android:textColor="#99FFFFFF"
            android:layout_marginEnd="6dp" />

        <TextView
            android:id="@+id/greeting_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bonne nuit"
            android:textSize="12sp"
            android:textColor="#99FFFFFF" />
            
        <View 
            android:layout_width="0dp" 
            android:layout_height="0dp" 
            android:layout_weight="1"/>
            
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Appuyez pour voir les détails"
            android:textSize="10sp"
            android:textColor="#80FFFFFF"
            android:textStyle="italic" />

    </LinearLayout>

</RelativeLayout>
